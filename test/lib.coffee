expect = require('chai').expect

describe 'given a record set result in KatyQuery format', ->
  
  describe 'with no joins', ->
    it 'should bind as expected', ->

  describe 'with a one to one inner join', ->
    it 'should bind as expected', ->

  describe 'with a one to many inner join', ->
    it 'should bind as expected', ->

  describe 'with a many to many inner join', ->
    it 'should bind as expected', ->


